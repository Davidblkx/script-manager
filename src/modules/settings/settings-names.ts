export const settingKeys = {
  editor: {
    files: {
      tool: 'editor.files.tool',
    },
    folder: {
      tool: 'editor.folder.tool',
    },
    diff: {
      tool: 'editor.diff.tool',
    },
  }
}
